from django.urls import path
from . import views

app_name = 'tracking'

urlpatterns = [
    # Placeholder for tracking endpoints
    # path('', views.TrackingListView.as_view(), name='tracking-list'),
]
